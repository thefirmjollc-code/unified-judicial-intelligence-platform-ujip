(() => {
const doc=document.documentElement;
const page=document.body.dataset.page||'home';
let lang=localStorage.getItem('ujip-lang')||'ar';
const setLang=(next)=>{lang=next;doc.lang=lang;doc.dir=lang==='ar'?'rtl':'ltr';document.querySelectorAll('[data-ar][data-en]').forEach(el=>{el.innerHTML=el.dataset[lang]});document.querySelectorAll('[data-placeholder-ar]').forEach(el=>{el.placeholder=el.dataset['placeholder'+(lang==='ar'?'Ar':'En')]});document.querySelectorAll('[data-lang-label]').forEach(el=>el.textContent=lang==='ar'?'EN':'AR');localStorage.setItem('ujip-lang',lang);window.dispatchEvent(new CustomEvent('ujip:lang',{detail:lang}))};
window.UJIP={getLang:()=>lang,setLang,toast:(text)=>{const t=document.querySelector('.toast');if(!t)return;t.textContent=text;t.classList.add('show');clearTimeout(t._x);t._x=setTimeout(()=>t.classList.remove('show'),2600)}};
setLang(lang);
document.querySelectorAll('[data-lang-toggle]').forEach(b=>b.addEventListener('click',()=>setLang(lang==='ar'?'en':'ar')));
const menu=document.querySelector('.mobile-nav');document.querySelectorAll('[data-menu]').forEach(b=>b.addEventListener('click',()=>menu?.classList.toggle('open')));document.querySelectorAll('.mobile-nav a').forEach(a=>a.addEventListener('click',()=>menu?.classList.remove('open')));
// active nav
for(const a of document.querySelectorAll('[data-page-link]')) if(a.dataset.pageLink===page)a.classList.add('active');
// cinematic page transitions
const layer=document.querySelector('.transition-layer');document.querySelectorAll('a[data-transition]').forEach(a=>a.addEventListener('click',e=>{if(e.metaKey||e.ctrlKey||a.target==='_blank'||a.hasAttribute('download'))return;const url=a.getAttribute('href');if(!url||url.startsWith('#'))return;e.preventDefault();layer?.classList.add('active');setTimeout(()=>location.href=url,650)}));
window.addEventListener('pageshow',()=>setTimeout(()=>layer?.classList.remove('active'),120));
// reveal observer
const obs=new IntersectionObserver(es=>es.forEach(e=>{if(e.isIntersecting){e.target.classList.add('in');obs.unobserve(e.target)}}),{threshold:.12});document.querySelectorAll('.reveal').forEach(el=>obs.observe(el));
// ambient mouse light
window.addEventListener('pointermove',e=>{doc.style.setProperty('--mx',e.clientX+'px');doc.style.setProperty('--my',e.clientY+'px')},{passive:true});
// custom cursor
if(matchMedia('(pointer:fine)').matches&&!matchMedia('(prefers-reduced-motion:reduce)').matches){const dot=document.querySelector('.cursor-dot'),ring=document.querySelector('.cursor-ring'),label=document.querySelector('.cursor-label');let x=innerWidth/2,y=innerHeight/2,rx=x,ry=y;const move=e=>{x=e.clientX;y=e.clientY;dot.style.opacity=ring.style.opacity=1;dot.style.transform=`translate3d(${x-2.5}px,${y-2.5}px,0)`};addEventListener('mousemove',move);const tick=()=>{rx+=(x-rx)*.16;ry+=(y-ry)*.16;ring.style.transform=`translate3d(${rx-17}px,${ry-17}px,0)`;requestAnimationFrame(tick)};tick();document.querySelectorAll('a,button,input,[data-cursor]').forEach(el=>{el.addEventListener('mouseenter',()=>{ring.classList.add('hover');label.textContent=el.dataset.cursor||'OPEN'});el.addEventListener('mouseleave',()=>{ring.classList.remove('hover');label.textContent=''})})}
// preloader: five-second first-entry cinematic boot
const pre=document.querySelector('.preloader');if(pre){const first=!sessionStorage.getItem('ujip-booted');const duration=first?5000:1100;const start=performance.now();const bar=pre.querySelector('.pre-bar i'),pct=pre.querySelector('.pre-percent'),step=pre.querySelector('.pre-step');const steps={ar:['تهيئة رسم العلاقات القضائية','مزامنة موصلات الالتزامات المالية','معايرة محرك الذكاء القابل للتفسير','تفعيل الحوكمة البشرية وسجل التدقيق','المنصة جاهزة للمراجعة القضائية'],en:['Initializing judicial knowledge graph','Synchronizing financial obligation connectors','Calibrating explainable intelligence engine','Activating human governance and audit trail','Platform ready for judicial review']};function frame(now){const p=Math.min(1,(now-start)/duration);bar.style.width=(p*100).toFixed(0)+'%';pct.textContent=String(Math.floor(p*100)).padStart(2,'0')+' / 100';const i=Math.min(4,Math.floor(p*5));step.textContent=steps[lang][i];if(p<1)requestAnimationFrame(frame);else{sessionStorage.setItem('ujip-booted','1');setTimeout(()=>pre.classList.add('done'),160)}}requestAnimationFrame(frame);window.addEventListener('ujip:lang',e=>{const elapsed=Math.min(1,(performance.now()-start)/duration);step.textContent=steps[e.detail][Math.min(4,Math.floor(elapsed*5))]});
// canvas neural network
const c=pre.querySelector('canvas');if(c){const ctx=c.getContext('2d');let pts=[];const resize=()=>{c.width=innerWidth*devicePixelRatio;c.height=innerHeight*devicePixelRatio;c.style.width=innerWidth+'px';c.style.height=innerHeight+'px';ctx.setTransform(devicePixelRatio,0,0,devicePixelRatio,0,0);pts=Array.from({length:Math.min(90,Math.floor(innerWidth/15))},()=>({x:Math.random()*innerWidth,y:Math.random()*innerHeight,vx:(Math.random()-.5)*.16,vy:(Math.random()-.5)*.16}))};resize();addEventListener('resize',resize);const draw=()=>{if(pre.classList.contains('done'))return;ctx.clearRect(0,0,innerWidth,innerHeight);for(const p of pts){p.x+=p.vx;p.y+=p.vy;if(p.x<0||p.x>innerWidth)p.vx*=-1;if(p.y<0||p.y>innerHeight)p.vy*=-1}for(let i=0;i<pts.length;i++){for(let j=i+1;j<pts.length;j++){const a=pts[i],b=pts[j],d=Math.hypot(a.x-b.x,a.y-b.y);if(d<125){ctx.strokeStyle=`rgba(70,217,255,${(1-d/125)*.12})`;ctx.beginPath();ctx.moveTo(a.x,a.y);ctx.lineTo(b.x,b.y);ctx.stroke()}}ctx.fillStyle='rgba(70,217,255,.28)';ctx.beginPath();ctx.arc(pts[i].x,pts[i].y,1.2,0,Math.PI*2);ctx.fill()}requestAnimationFrame(draw)};draw()}}
})();
